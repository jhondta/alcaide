defmodule Alcaide do
  @moduledoc """
  Alcaide deploys Phoenix applications to FreeBSD servers using Jails.

  Similar to Kamal, it uses a single configuration file (`deploy.exs`) and
  a single command (`alcaide deploy`) to deploy your application with
  blue/green rotation and zero downtime.
  """
end
